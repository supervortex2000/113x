diff --git a/arch/arm64/configs/meson64_a64_defconfig b/arch/arm64/configs/meson64_a64_defconfig
index e5b748a..353f2d4 100644
--- a/arch/arm64/configs/meson64_a64_defconfig
+++ b/arch/arm64/configs/meson64_a64_defconfig
@@ -1,3 +1,8 @@
+CONFIG_VIDEO_VIVID=y
+CONFIG_V4L_TEST_DRIVERS=y
+CONFIG_FONT_SUPPORT=y
+CONFIG_FONT_8x16=y
+CONFIG_VIDEO_V4L2_TPG=y
 CONFIG_POSIX_MQUEUE=y
 CONFIG_AUDIT=y
 CONFIG_GENERIC_IRQ_DEBUGFS=y
@@ -422,6 +427,7 @@ CONFIG_USB_CONFIGFS_F_FS=y
 CONFIG_USB_CONFIGFS_F_MIDI=y
 CONFIG_USB_CONFIGFS_F_HID=y
 CONFIG_USB_CONFIGFS_F_UVC=y
+CONFIG_USB_CONFIGFS_F_UAC2=y
 CONFIG_USB_CONFIGFS_F_PRINTER=y
 CONFIG_MMC=y
 CONFIG_MMC_BLOCK_MINORS=32
diff --git a/arch/arm64/configs/meson64_a64_smarthome_defconfig b/arch/arm64/configs/meson64_a64_smarthome_defconfig
index a009c81..bde111a 100644
--- a/arch/arm64/configs/meson64_a64_smarthome_defconfig
+++ b/arch/arm64/configs/meson64_a64_smarthome_defconfig
@@ -316,6 +316,7 @@ CONFIG_USB_CONFIGFS_RNDIS=y
 CONFIG_USB_CONFIGFS_F_FS=y
 CONFIG_USB_CONFIGFS_F_MIDI=y
 CONFIG_USB_CONFIGFS_F_UVC=y
+CONFIG_USB_CONFIGFS_F_UAC2=y
 CONFIG_MMC=y
 CONFIG_MMC_BLOCK_MINORS=32
 CONFIG_MMC_SDHCI=y
diff --git a/drivers/amlogic/usb/dwc_otg/310/dwc_otg_cil.h b/drivers/amlogic/usb/dwc_otg/310/dwc_otg_cil.h
index 66420ee..659e9c7 100644
--- a/drivers/amlogic/usb/dwc_otg/310/dwc_otg_cil.h
+++ b/drivers/amlogic/usb/dwc_otg/310/dwc_otg_cil.h
@@ -817,7 +817,7 @@ struct ep_tx_fifo_info {
 };
 
 #define TX_EP_FIFO_NUM 5
-#define ISO_EP_FIFO_LEN 256
+#define ISO_EP_FIFO_LEN 192
 #define INTR_EP_FIFO_LEN 16
 #define BULK_EP_FIFO_LEN 128
 
diff --git a/drivers/amlogic/usb/dwc_otg/310/dwc_otg_pcd.c b/drivers/amlogic/usb/dwc_otg/310/dwc_otg_pcd.c
index cf7f7e5..b211f56 100644
--- a/drivers/amlogic/usb/dwc_otg/310/dwc_otg_pcd.c
+++ b/drivers/amlogic/usb/dwc_otg/310/dwc_otg_pcd.c
@@ -1566,7 +1566,10 @@ static uint32_t aml_assign_tx_fifo
 	u32 ep_fifo_size = 0;
 
 	if (ep->dwc_ep.type == UE_ISOCHRONOUS) {
-		ep_fifo_size = ISO_EP_FIFO_LEN;
+		if (ep->desc->bInterval == 0x1)
+			ep_fifo_size = ISO_EP_FIFO_LEN;
+		else
+			ep_fifo_size = 64;
 		core_if->iso_ep_cnt++;
 	}
 
diff --git a/drivers/amlogic/usb/dwc_otg/310/dwc_otg_pcd_intr.c b/drivers/amlogic/usb/dwc_otg/310/dwc_otg_pcd_intr.c
index 4a04231..e65a3a0 100644
--- a/drivers/amlogic/usb/dwc_otg/310/dwc_otg_pcd_intr.c
+++ b/drivers/amlogic/usb/dwc_otg/310/dwc_otg_pcd_intr.c
@@ -3541,7 +3541,10 @@ static inline void handle_in_ep_disable_intr(dwc_otg_pcd_t *pcd,
 
 	if (dwc_ep->type == DWC_OTG_EP_TYPE_ISOC) {
 		dwc_otg_flush_tx_fifo(core_if, dwc_ep->tx_fifo_num);
-		complete_ep(ep);
+		if (dwc_ep->is_in && dwc_ep->bInterval > 1)
+			start_next_request(ep);
+		else
+			complete_ep(ep);
 		return;
 	}
 
diff --git a/drivers/usb/gadget/function/f_uac2.c b/drivers/usb/gadget/function/f_uac2.c
index d582921..0bcca73 100644
--- a/drivers/usb/gadget/function/f_uac2.c
+++ b/drivers/usb/gadget/function/f_uac2.c
@@ -610,6 +610,7 @@ afunc_set_alt(struct usb_function *fn, unsigned intf, unsigned alt)
 	struct f_uac2 *uac2 = func_to_uac2(fn);
 	struct usb_gadget *gadget = cdev->gadget;
 	struct device *dev = &gadget->dev;
+	struct f_uac2_opts *uac2_opts;
 	int ret = 0;
 
 	/* No i/f has more than 2 alt settings */
@@ -636,11 +637,14 @@ afunc_set_alt(struct usb_function *fn, unsigned intf, unsigned alt)
 			u_audio_stop_capture(&uac2->g_audio);
 	} else if (intf == uac2->as_in_intf) {
 		uac2->as_in_alt = alt;
-
-		if (alt)
+		uac2_opts = container_of(fn->fi, struct f_uac2_opts, func_inst);
+		if (alt) {
 			ret = u_audio_start_playback(&uac2->g_audio);
-		else
+			uac2_opts->p_status = 1;
+		} else {
 			u_audio_stop_playback(&uac2->g_audio);
+			uac2_opts->p_status = 0;
+		}
 	} else {
 		dev_err(dev, "%s:%d Error!\n", __func__, __LINE__);
 		return -EINVAL;
@@ -907,6 +911,7 @@ UAC2_ATTRIBUTE(c_chmask);
 UAC2_ATTRIBUTE(c_srate);
 UAC2_ATTRIBUTE(c_ssize);
 UAC2_ATTRIBUTE(req_number);
+UAC2_ATTRIBUTE(p_status);
 
 static struct configfs_attribute *f_uac2_attrs[] = {
 	&f_uac2_opts_attr_p_chmask,
@@ -916,6 +921,7 @@ static struct configfs_attribute *f_uac2_attrs[] = {
 	&f_uac2_opts_attr_c_srate,
 	&f_uac2_opts_attr_c_ssize,
 	&f_uac2_opts_attr_req_number,
+	&f_uac2_opts_attr_p_status,
 	NULL,
 };
 
@@ -954,6 +960,7 @@ static struct usb_function_instance *afunc_alloc_inst(void)
 	opts->c_srate = UAC2_DEF_CSRATE;
 	opts->c_ssize = UAC2_DEF_CSSIZE;
 	opts->req_number = UAC2_DEF_REQ_NUM;
+	opts->p_status = UAC2_DEF_PSTATUS;
 	return &opts->func_inst;
 }
 
diff --git a/drivers/usb/gadget/function/f_uvc.c b/drivers/usb/gadget/function/f_uvc.c
index f06e6da..092d35d 100644
--- a/drivers/usb/gadget/function/f_uvc.c
+++ b/drivers/usb/gadget/function/f_uvc.c
@@ -721,6 +721,7 @@ uvc_function_bind(struct usb_configuration *c, struct usb_function *f)
 		INFO(cdev, "Unable to allocate streaming EP\n");
 		goto error;
 	}
+	ep->maxpacket =  uvc_hs_streaming_ep.wMaxPacketSize;
 	uvc->video.ep = ep;
 
 	uvc_fs_streaming_ep.bEndpointAddress = uvc->video.ep->address;
diff --git a/drivers/usb/gadget/function/u_uac2.h b/drivers/usb/gadget/function/u_uac2.h
index 8362ee5..72d1de9 100644
--- a/drivers/usb/gadget/function/u_uac2.h
+++ b/drivers/usb/gadget/function/u_uac2.h
@@ -19,9 +19,10 @@
 #define UAC2_DEF_PSRATE 48000
 #define UAC2_DEF_PSSIZE 2
 #define UAC2_DEF_CCHMASK 0x3
-#define UAC2_DEF_CSRATE 64000
+#define UAC2_DEF_CSRATE 48000
 #define UAC2_DEF_CSSIZE 2
 #define UAC2_DEF_REQ_NUM 2
+#define UAC2_DEF_PSTATUS 0
 
 struct f_uac2_opts {
 	struct usb_function_instance	func_inst;
@@ -36,6 +37,7 @@ struct f_uac2_opts {
 
 	struct mutex			lock;
 	int				refcnt;
+	int				p_status;
 };
 
 #endif
